User documents
   ↓
loaders.py          (file → text + metadata)
   ↓
chunker.py          (text → chunks)
   ↓
embedder.py         (chunk → Gemini embedding)
   ↓
vectordb.py         (store in Pinecone)
   ↓
main.py             (sirf trigger)
   ↓
 llm for question answers



User query
   ↓
embed (Cohere)   ← already built
   ↓
Pinecone search ← already built
   ↓
retrieved chunks
   ↓
LLM (Groq LLaMA-3)
   ↓
Answer


🚦Order we will follow (important)

1️⃣ llm_client.py → connect LLM
2️⃣ prompt.py → RAG prompt
3️⃣ rag_chain.py → glue logic
4️⃣ retriever.py → query wrapper (if not done)
5️⃣ main.py → chatbot loop
6️⃣ Streamlit UI

Har file ka role (1-line each)

loaders.py
Different file types ko ek common format me convert karta hai

chunker.py
Long text ko context-safe chunks me todta hai

embedder.py
Gemini API se semantic embeddings banata hai

vectordb.py
Pinecone me vectors store karta hai

ingest_pipeline.py
Sab stages ko jodta hai (real pipeline)

main.py
Entry point (run yahin se)